from dbm.ndbm import library


library(dplyr)

library(ggplot2)

library(tidyr)

library(plotly)

library(purrr)

library(treemapify)

library(reshape2)

library(gganimate)

library(ggridges)
df <- read.csv("tv_shows.csv")

head(df)